name = "hersymispiste"
compatibility_date = "2025-12-20"

[assets]
directory = "./public"

# This project is a static Hugo site deployed as a Cloudflare Worker with
# generated assets. The site is built into ./public and then deployed
# as Worker assets.