name = "hersymispiste"
compatibility_date = "2025-12-20"
pages_build_output_dir = "./public"

# This project is a static Hugo site deployed through Cloudflare Pages.
# Cloudflare Pages builds the site from the repository and publishes the
# generated ./public directory. Do not configure this as a Worker entry point.