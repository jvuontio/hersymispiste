name = "hersymispiste"
compatibility_date = "2025-12-20"
pages_build_output_dir = "./public"

# Cloudflare Pages static-site deployment for the Hugo build output.
# The site is built locally by build.sh and then published from ./public.
# Do not use `wrangler deploy` for this project; use Pages deploy or the Pages GitHub integration.